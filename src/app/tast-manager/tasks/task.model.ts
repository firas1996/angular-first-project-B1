export interface TheTask {
  id: number;
  userId: string;
  title: string;
  description: string;
  deadline: string;
}

export interface TaskData {
  title: string;
  description: string;
  deadline: string;
}
